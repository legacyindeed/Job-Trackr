{
    "version": 2,
    "builds": [
        {
            "src": "portal/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/portal/$1"
        }
    ]
}